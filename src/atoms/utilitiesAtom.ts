import { atom } from 'jotai';

export const selectedUpgradeAtom = atom<string | null>(null);
export const selectedCardAtom = atom<string | null>(null);
export const selectedLabAtom = atom<string | null>(null);